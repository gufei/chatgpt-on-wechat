syntax = "proto3";
package Jubo.JuLiao.IM.Wx.Proto; //命名空间约定

message TriggerCircleMsgPushTaskMessage {
    string WeChatId = 1; // 商家所属微信号
    bool OnlyComment = 2; // 缺省获取全部
    bool GetAll = 3; // 缺省只获取未读消息
    int64 TaskId = 4;
}