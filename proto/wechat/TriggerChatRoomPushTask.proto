syntax = "proto3";
package Jubo.JuLiao.IM.Wx.Proto; //命名空间约定

// 注意MessageType大小写： TriggerChatroomPushTask = 1210; //触发手机推送群聊列表
message TriggerChatRoomPushTaskMessage {
    string WeChatId = 1; // 商家所属微信号
    int32 Flag = 2; // Flag = 1:不推送成员信息（ChatRoomMembersNotice)
    int64 TaskId = 3;
}