syntax = "proto3";
package Jubo.JuLiao.IM.Wx.Proto; //命名空间约定

message TriggerChatMsgIdsPushTaskMessage {
    string WeChatId = 1; // 所属微信号
    int64 StartTime = 3; // 开始同步的消息时间，UTC毫秒，最多一天
    int64 EndTime = 4;   // 结束同步的消息时间，UTC毫秒,一般为当前时间
}